language: julia
os:
  - linux
julia:
  - 1.0
notifications:
  email: false
after_success:
- julia -e 'using Pkg; Pkg.add(["Coverage"])'
- julia -e 'using Pkg, Coverage, ILU; cd(joinpath(dirname(pathof(ILU)), "..")); Codecov.submit(process_folder())'